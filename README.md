
# Toxicité JV — corrections TSX
- Correction clé : `src/lib/consent.tsx` (JSX ➜ fichier TSX)
- Lancer: `npm i && npm run dev` puis http://localhost:3000

Pub providers via `.env.local` (GAM ou AdSense) comme avant.
